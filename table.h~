#include <stdio.h>

#define LINHA 5
#define COLUNA 4



void printTable(char table[5][4])
{
	int i;
	int j;

	for(i = 0; i < 5; i++)
	{
		for(j = 0; j < 8; j++)
		{
			printf("%c", table[i][j]);
		}
		printf("\n");	
	}
}
